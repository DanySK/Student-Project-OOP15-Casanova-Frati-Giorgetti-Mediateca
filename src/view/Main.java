package view;

public class Main {
	public void main(final String[] args) {
		View V = new ViewImpl();
		V.startView();
		/*
		 * final JFrame mainFrame = new JFrame(); final Menu menu = new
		 * MenuImpl(Main.SCREEN_LENGHT, Main.SCREEN_WIDTH, mainFrame); final
		 * SelectAccountType choose = new SelectAccountTypeImpl(); final
		 * UserLogin login = new UserLoginImpl();
		 *
		 * mainFrame.setTitle("Mediateca");
		 * mainFrame.setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);
		 * mainFrame.setSize(Main.SCREEN_LENGHT, Main.SCREEN_WIDTH);
		 * mainFrame.add(menu.getPanel());
		 *
		 * mainFrame.setVisible(true);
		 */
	}
}
