package utils;

/**
 * Simple enum to know which information we want from user.
 *
 * @author Luca
 *
 */
public enum UserInfo {
	/**
	 * enum of user information.
	 */
	NAME, SURNAME, USERNAME, PASSWORD, BIRTHDATE, EMAIL, TELEPHONE_NUMBER
}
