package utils;

/**
 * enum for Item Information we can get and set.
 *
 * @author Luca Giorgetti
 *
 */
public enum TypeItemInfo {
	/**
	 * enum of item infomration.
	 */
	TITLE, AUTHOR, PRODUCER, RELEASE_YEAR, GENRE, IMAGE, DURATION, COLOR, ISBN, LANGUAGE, TYPE, RELEASE_NUMBER, COPIES_NUMBER
}